{
  "name": "@eui-mp-ui/components",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "cross-env NODE_ENV=test pnpm --filter @eui-mp-ui/hooks build && vitest --run --coverage",
    "test:watch":"cross-env NODE_ENV=test pnpm --filter @eui-mp-ui/hooks build && vitest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
