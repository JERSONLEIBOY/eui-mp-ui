import { ErButton, ErIcon, ErCollapse, ErCollapseItem, ErAlert, ErTooltip, } from "@eui-mp-ui/components";

import type { Plugin } from "vue";

export default [ErButton, ErIcon, ErCollapse, ErCollapseItem, ErAlert, ErTooltip] as Plugin[];